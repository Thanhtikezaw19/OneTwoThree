ruby:
  limit = TwoNumber.first.limit
  two_numbers = TwoNumber.all
.over-panel
  table.tbl.mt-2
    thead.over-hdr
      tr
        td #
        td Bet
        td Amount
    .table-over
      - index = 1
      - two_numbers.each do |tn|
        - if limit && tn.amount > limit
          tbody
            td #{index}
            td #{tn.numbers}
            td #{tn.amount - limit}
          - index += 1
css:
  .over-hdr {
    border: 2px solid black;
    background-color: grey;
    color: #00ffff;
  }
